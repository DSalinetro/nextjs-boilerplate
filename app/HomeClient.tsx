// app/HomeClient.tsx  (CLIENT component)
'use client';

import { useEffect, useState } from 'react';
// import other client-only libs here (framer-motion, etc.)

export default function HomeClient() {
  // put your hooks and UI here (the stuff that was in app/page.tsx)

  return (
    <main>
      {/* your homepage JSX */}
    </main>
  );
}
